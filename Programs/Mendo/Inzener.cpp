#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int x, y, n; cin >> x >> y >> n;
    if (30*y < 40*x) {
        cout << "DA";
    }
    else {
        cout << "NE";
    }
}
